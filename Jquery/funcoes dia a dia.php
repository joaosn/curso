var valor = $.trim($('#algo').html()); // passar direto para tira espaços

$.trim(valor);// para tirar epaços en branco asim quando vc define  variavel.

$('li').each(function(){
	alert($(this).html()); // esssa faz loop nas coisa no caso vai la aletr en todos item na lista
});

$.type(); // para decobrir tipo da varialvel so passar ela como parametro

para verificr as coisa se existe ou nao usar o if e  

do jquery para nativo $('li').eq()[]; assim eu passo do jquery para java normal