$(function() {
	$('.div').hide(); // seleciono elemento e dps dou .hide/.show
	$('.div').toggle(); // faz 2 serviço ja.. toggle use quase todos 
    $('.div').fadeOut(); // some numa leve animação tbm .fadeout/.fadeIn
    $('.div').fadeTo('', )// 1 parametro velocidae 2 intensidade q objeto volta
});
